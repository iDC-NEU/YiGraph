prompt_select_graph_algorithm_str_zh = """
你是一位图算法专家，擅长根据自然语言问题和从图算法论文知识库中检索到的相关上下文，判断最合适的图算法。
现在我会提供一个**输入问题**和几段与该问题相关的**检索上下文**。这些上下文是一个由多个段落构成的字符串列表（List[str]），每段内容来自图算法相关论文的文本片段。
请你根据两者的信息，推理并输出一个最合适的图算法名称。

### 任务要求:
1.你的任务是从所有图算法中（如：PageRank、Dijkstra、Louvain、Connected Components、Betweenness Centrality、GCN、GAT等），选择一个最适合解决该问题的算法名称
2.如果上下文中明确指出某一算法适用于该类问题，优先选用该算法
3.如果没有明确指出，请结合问题类型和上下文语义进行推理，选择最合适的算法
4.只输出算法名称，不要解释理由、不输出其他内容。 输出如下格式：

**输出格式:**
```json
{{
    "algorithm": # type: str
}}
```

### 示例:
**输入问题:**
在 Facebook 社交网络中，用户Anna最近被举报为恶意账号，我们想找出可能与她组成“恶意账号团伙”的其他账号，有哪些账号与她在同一个社交连通群中？
**检索上下文:**
[
    "在用户关系图中，每个节点表示一个用户，边表示双向好友关系。为了识别潜在的协同行为群体，常用的方法之一是找到图中的“连通子图”。Connected Components 算法可用于无向图中划分出所有互相可达的节点集合，适合发现局部紧密联系的账号团伙，尤其在恶意行为溯源和社交异常检测中被广泛使用。通过计算连通分量，可以快速识别与某个已知异常用户处于同一个群体中的所有账号，无需遍历全图结构，计算效率高，适用于大规模社交网络。",
    "Connected Components 是图论中的基础算法之一，主要用于在无向图中识别所有互相可达的节点集合。每一个连通分量代表图中的一个子图，其中任意两个节点之间都存在路径连接。该算法广泛应用于社交网络分析、聚类检测、生物网络中的功能模块识别，以及大规模图中的孤立社群发现等任务。其计算效率高，适合用于结构已知的静态图和稀疏图中。"
]

**输出:**
```json
{{
    "algorithm": "Connected Components"
}}
```

### 指令:
**请根据下方提供的输入问题与检索上下文，推理出最合适的图算法，并以标准格式输出：

**输入问题:**
{input_question}

**检索上下文:**
{context}

**Output:**

"""


rewrite_query_prompt_zh = """
你是一位图分析和查询优化专家。你的任务是将模糊或含糊的用户查询重写为具体、明确、可执行的任务，使其与系统的算法能力相匹配。

## 背景信息
你将收到：
1. **original_query**: 用户的原始问题，可能包含模糊概念或不明确的需求
2. **algorithm_library_info**: 系统中可用的任务类型和算法信息
3. **dataset_info** (可选): 当前图数据集的schema信息

## 你的任务
将模糊查询转换为具体的、分步骤的查询，要求：
1. 将模糊概念映射到算法库中的具体算法类别
2. 将复杂需求分解为清晰、可执行的子任务
3. 使用与可用算法一致的精确术语
4. 保持原始意图的同时使其可操作

## 算法库上下文
系统支持以下任务类型：
{algorithm_library_info}

## 数据集上下文（如果可用）
{dataset_info}

## 重写指南
1. **将模糊概念映射到算法**：
   - "洗钱行为" → 中心性分析（影响力检测）+ 聚类（环路检测）
   - "异常交易" → 社区检测 + 路径分析
   - "高风险用户" → 中心性排名 + 阈值过滤

2. **对指标和阈值要具体**：
   - 不要说"有影响力的"，而要说"PageRank分数排名前10%"
   - 不要说"频繁交易"，而要说"交易次数 > 阈值"
   - 不要说"可疑路径"，而要说"环形交易路径"

3. **分解复杂任务**：
   - 将检测与分析分开
   - 将过滤与计算分开
   - 明确依赖关系

4. **使用算法感知的语言**：
   - 引用具体的图结构（路径、环、社区）
   - 使用可测量的标准（top-K、基于阈值、排名）
   - 明确指定范围（社区内、节点间、全局图）

## 转换示例

**原始查询：**
"最近我发现用户Anna与一些"骡子"账户进行了频繁的交易，我想围绕她进行检查，请帮我检查是否存在洗钱行为。列出所有潜在的洗钱路径，估算已经有多少现金可能已经被非法转出，并指出其中交易金额最大的账户。"

**重写后的查询：**
"检测Anna的影响力是否排在所有用户的前10%，如果是，说明Anna为高风险用户。之后列出Anna所在的环形交易路径，计算交易路径上的的金额总和，找出路径中交易金额最大的账户。"

**变更说明：**
- "洗钱行为" → 具体化为："影响力排名前10%"（中心性分析）+ "环形交易路径"（环路检测）
- "频繁交易" → 可测量："影响力排名"（可通过PageRank/中心性计算）
- "潜在洗钱路径" → 具体化："环形交易路径"（交易图中的环）
- "估算现金转出" → 具体化："计算各个路径的金额总和"（路径边上的聚合）
- "交易金额最大的账户" → 明确："每个路径中交易金额最大的账户"（每条路径的最大值聚合）

## 输出格式
返回一个JSON对象，结构如下：
```json
{{
    "rewritten_query": "<重写后的具体查询>",
    "reasoning": "<说明做了哪些改变以及为什么>",
    "mapped_concepts": [
        {{
            "original_concept": "<原始查询中的模糊概念>",
            "mapped_to": "<映射到的具体算法/任务类型>",
            "reason": "<为什么这个映射有意义>"
        }}
    ]
}}
```

## 现在请重写查询

**原始查询：**
{original_query}

**算法库信息：**
{algorithm_library_info}

**数据集信息：**
{dataset_info}

请将查询重写为具体可执行的形式。只输出有效的JSON格式。
"""


expert_subqueries_with_algorithms_prompt_zh = """
你是一名图分析工作流专家。你的任务是把专家给出的自然语言分析指令，转换为可执行的 DAG 子查询计划。

## 输入
你将收到：
1. expert_instruction: 专家输入的自然语言指令（可能包含任务步骤、依赖关系、算法偏好）
2. algorithm_library_info: 当前系统算法库信息（任务类型与可用算法）
3. dataset_info: 当前数据集结构信息（可选）

## 核心要求
1. 输出 JSON，且只能输出 JSON，不要输出解释文字。
2. 输出结构必须是：
{{
  "subqueries": [
    {{
      "id": "q1",
      "query": "子问题描述",
      "depends_on": [],
      "algorithm": "算法ID"
    }}
  ],
  "instruction_algorithm_adjustments": [
    {{
      "mentioned_algorithm": "专家指令中提到的算法名",
      "replacement_algorithm": "系统最终采用的替代算法ID（若无则为空字符串）",
      "recommendations": ["推荐算法1", "推荐算法2"],
      "reason": "替换理由"
    }}
  ],
  "instruction_algorithm_adjustment_summary": "给用户的简短说明，可为空字符串"
}}
3. 每个 subquery 必须包含 `algorithm` 字段，且算法应来自算法库。
4. `depends_on` 必须是无环依赖；先决任务应在前，后续任务依赖前置任务。
5. 如果专家明确指定了算法，优先使用该算法（前提是算法库可用）。
6. 如果专家没有指定算法，请根据子问题语义从算法库选择最合适算法。
7. `query` 要具体可执行，不要空泛描述。
8. 如果专家指令中提到了算法库不存在的算法（如 SPFA），请在 `instruction_algorithm_adjustments` 中给出替换与推荐。
9. 如果不存在替换情况，`instruction_algorithm_adjustments` 输出空数组，`instruction_algorithm_adjustment_summary` 输出空字符串。

## 额外约束
1. id 统一使用 q1, q2, q3... 连续编号。
2. 不要生成无意义步骤。
3. 若一个问题只需一步，也必须给出一个 subquery，包含 algorithm。

## 输出示例
{{
  "subqueries": [
    {{
      "id": "q1",
      "query": "识别节点23所在社区的节点集合",
      "depends_on": [],
      "algorithm": "louvain_communities"
    }},
    {{
      "id": "q2",
      "query": "在该社区子图中计算影响力最高的前10个节点",
      "depends_on": ["q1"],
      "algorithm": "pagerank"
    }}
  ],
  "instruction_algorithm_adjustments": [],
  "instruction_algorithm_adjustment_summary": ""
}}

## 输入内容
expert_instruction:
{expert_instruction}

algorithm_library_info:
{algorithm_library_info}

dataset_info:
{dataset_info}

请严格按 JSON 输出。
"""

plan_subqueries_prompt_zh = """
**角色**：你是一名专门负责分解复杂查询的 AI 助手。你的任务是将一个复杂问题拆解为多个具有逻辑依赖关系的子查询。

**可用资源**：
1. **完整算法库**：支持 NetworkX 中的图算法，包括但不限于：
- 中心性指标：PageRank
- 寻找环路算法：find_cycle
- 以及 NetworkX 支持的其他图算法

2. **强大的后处理能力**：可以对任意图算法的结果执行以下操作：
- 排序、过滤、交集/并集
- 数学运算：加权求和、归一化、标准化
- 统计分析：最大值、最小值、平均值、百分位数
- 逻辑操作：条件过滤、多结果融合

**分解原则**：
1. **单一算法原则**：每个子问题只使用一种核心图算法。
2. **流水线思维**：前序算法的结果经过后处理后，可以作为后续算法的输入。

**业务流程**：
你分解时要根据解决该问题时的业务流程。
比如，解决洗钱行为问题时，业务流程是：
1. 先计算用户的影响力分数并统计排名。
2. 然后列出用户所在的环形交易路径。该步骤依赖1的结果
3. 计算交易路径上的的金额总和。该步骤依赖2的结果
4. 找出路径中交易金额最大的账户。该步骤需要依赖2的结果
再比如下面示例中欺诈社区检测的业务流程是：
1. 先检测用户是否为欺诈用户。
2. 然后列出用户所在的欺诈社区。该步骤依赖1的结果
3. 计算欺诈社区中以用户为中心的可疑交易路径。该步骤依赖2的结果
4. 判断非法转出的现金金额。该步骤需要依赖步骤2和步骤3的结果

每个子查询都必须包含唯一 ID（例如 `"q1"`、`"q2"`）、子查询文本本身，以及一个 `depends_on` 列表，用于指明在回答当前子查询前，必须先解决哪些其他子查询。

请根据逻辑必要性推断依赖关系。如果回答某个子查询需要另一个子查询的结果，则在 `depends_on` 中写明对应的 ID。依赖关系应基于前置条件和信息流。

示例 1：
输入查询：
“最近我发现 Anna 的交易行为异常，她可能是一个潜在的欺诈用户。我想找出围绕她的潜在欺诈社区，给出可能的可疑交易路径，并判断大概有多少现金被非法转移了出去。”

输出：
{{
"subqueries": [
    {{
        "id": "q1",
        "query": "根据 Anna 的异常交易行为，判断她是否是欺诈用户。",
        "depends_on": []
    }},
    {{
        "id": "q2",
        "query": "找出以 Anna 为中心的潜在欺诈社区。",
        "depends_on": ["q1"]
    }},
    {{
        "id": "q3",
        "query": "Anna 相关的可能可疑交易路径有哪些？",
        "depends_on": ["q2"]
    }},
    {{
        "id": "q4",
        "query": "判断大概有多少现金被非法转移了出去。",
        "depends_on": ["q2", "q3"]
    }}
]
}}
现在，请根据以上说明和示例，对用户提供的新的复杂查询进行分解。你的输出必须仅为合法的 JSON 对象。用户查询为：{query}
"""