prompt_select_graph_algorithm_str_zh = """
你是一位图算法专家，擅长根据自然语言问题和从图算法论文知识库中检索到的相关上下文，判断最合适的图算法。
现在我会提供一个**输入问题**和几段与该问题相关的**检索上下文**。这些上下文是一个由多个段落构成的字符串列表（List[str]），每段内容来自图算法相关论文的文本片段。
请你根据两者的信息，推理并输出一个最合适的图算法名称。

### 任务要求:
1.你的任务是从所有图算法中（如：PageRank、Dijkstra、Louvain、Connected Components、Betweenness Centrality、GCN、GAT等），选择一个最适合解决该问题的算法名称
2.如果上下文中明确指出某一算法适用于该类问题，优先选用该算法
3.如果没有明确指出，请结合问题类型和上下文语义进行推理，选择最合适的算法
4.只输出算法名称，不要解释理由、不输出其他内容。 输出如下格式：

**输出格式:**
```json
{{
    "algorithm": # type: str
}}
```

### 示例:
**输入问题:**
在 Facebook 社交网络中，用户Anna最近被举报为恶意账号，我们想找出可能与她组成“恶意账号团伙”的其他账号，有哪些账号与她在同一个社交连通群中？
**检索上下文:**
[
    "在用户关系图中，每个节点表示一个用户，边表示双向好友关系。为了识别潜在的协同行为群体，常用的方法之一是找到图中的“连通子图”。Connected Components 算法可用于无向图中划分出所有互相可达的节点集合，适合发现局部紧密联系的账号团伙，尤其在恶意行为溯源和社交异常检测中被广泛使用。通过计算连通分量，可以快速识别与某个已知异常用户处于同一个群体中的所有账号，无需遍历全图结构，计算效率高，适用于大规模社交网络。",
    "Connected Components 是图论中的基础算法之一，主要用于在无向图中识别所有互相可达的节点集合。每一个连通分量代表图中的一个子图，其中任意两个节点之间都存在路径连接。该算法广泛应用于社交网络分析、聚类检测、生物网络中的功能模块识别，以及大规模图中的孤立社群发现等任务。其计算效率高，适合用于结构已知的静态图和稀疏图中。"
]

**输出:**
```json
{{
    "algorithm": "Connected Components"
}}
```

### 指令:
**请根据下方提供的输入问题与检索上下文，推理出最合适的图算法，并以标准格式输出：

**输入问题:**
{input_question}

**检索上下文:**
{context}

**Output:**

"""


rewrite_query_prompt_zh = """
你是一位图分析和查询优化专家。你的任务是将模糊或含糊的用户查询重写为具体、明确、可执行的任务，使其与系统的算法能力相匹配。

## 背景信息
你将收到：
1. **original_query**: 用户的原始问题，可能包含模糊概念或不明确的需求
2. **algorithm_library_info**: 系统中可用的任务类型和算法信息
3. **dataset_info** (可选): 当前图数据集的schema信息

## 你的任务
将模糊查询转换为具体的、分步骤的查询，要求：
1. 将模糊概念映射到算法库中的具体算法类别
2. 将复杂需求分解为清晰、可执行的子任务
3. 使用与可用算法一致的精确术语
4. 保持原始意图的同时使其可操作

## 算法库上下文
系统支持以下任务类型：
{algorithm_library_info}

## 数据集上下文（如果可用）
{dataset_info}

## 重写指南
1. **将模糊概念映射到算法**：
   - "洗钱行为" → 中心性分析（影响力检测）+ 聚类（环路检测）
   - "异常交易" → 社区检测 + 路径分析
   - "高风险用户" → 中心性排名 + 阈值过滤

2. **对指标和阈值要具体**：
   - 不要说"有影响力的"，而要说"PageRank分数排名前10%"
   - 不要说"频繁交易"，而要说"交易次数 > 阈值"
   - 不要说"可疑路径"，而要说"环形交易路径"

3. **分解复杂任务**：
   - 将检测与分析分开
   - 将过滤与计算分开
   - 明确依赖关系

4. **使用算法感知的语言**：
   - 引用具体的图结构（路径、环、社区）
   - 使用可测量的标准（top-K、基于阈值、排名）
   - 明确指定范围（社区内、节点间、全局图）

## 转换示例

**原始查询：**
"最近我发现用户Anna与一些"骡子"账户进行了频繁的交易，我想围绕她进行检查，请帮我检查是否存在洗钱行为。列出所有潜在的洗钱路径，估算已经有多少现金可能已经被非法转出，并指出其中交易金额最大的账户。"

**重写后的查询：**
"检测Anna的影响力是否排在所有用户的前10%，如果是，说明Anna为高风险用户。之后列出Anna所在的所有环形交易路径，分别计算各个路径的金额总和，分别列出每个路径中交易金额最大的账户。"

**变更说明：**
- "洗钱行为" → 具体化为："影响力排名前10%"（中心性分析）+ "环形交易路径"（环路检测）
- "频繁交易" → 可测量："影响力排名"（可通过PageRank/中心性计算）
- "潜在洗钱路径" → 具体化："环形交易路径"（交易图中的环）
- "估算现金转出" → 具体化："计算各个路径的金额总和"（路径边上的聚合）
- "交易金额最大的账户" → 明确："每个路径中交易金额最大的账户"（每条路径的最大值聚合）

## 输出格式
返回一个JSON对象，结构如下：
```json
{{
    "rewritten_query": "<重写后的具体查询>",
    "reasoning": "<说明做了哪些改变以及为什么>",
    "mapped_concepts": [
        {{
            "original_concept": "<原始查询中的模糊概念>",
            "mapped_to": "<映射到的具体算法/任务类型>",
            "reason": "<为什么这个映射有意义>"
        }}
    ]
}}
```

## 现在请重写查询

**原始查询：**
{original_query}

**算法库信息：**
{algorithm_library_info}

**数据集信息：**
{dataset_info}

请将查询重写为具体可执行的形式。只输出有效的JSON格式。
"""
